# Per-skill USDC pricing for x402 payment gate.
# Format: "$<amount>" — parsed by x402 PaymentOption.

default = "$0.01"

[skills]
diagram_create   = "$0.01"
shape_manage     = "$0.02"
connector_manage = "$0.02"
diagram_export   = "$0.03"
