# Per-skill USDC pricing for x402 payment gate.
# Format: "$<amount>" — parsed by x402 PaymentOption.

default = "$0.01"

[skills]
account_info       = "$0.01"
transaction_lookup = "$0.02"
block_inspect      = "$0.02"
network_status     = "$0.01"
validator_info     = "$0.03"
