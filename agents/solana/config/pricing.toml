# Per-skill USDC pricing for x402 payment gate.
# Format: "$<amount>" — parsed by x402 PaymentOption.

default = "$0.001"

[skills]
account_info       = "$0.001"
transaction_lookup = "$0.001"
block_inspect      = "$0.001"
network_status     = "$0.001"
validator_info     = "$0.001"
