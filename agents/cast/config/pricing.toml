# Per-skill USDC pricing for x402 payment gate.
# Format: "$<amount>" — parsed by x402 PaymentOption.

default = "$0.001"

[skills]
tx_decode       = "$0.001"
receipt_parse   = "$0.001"
trace           = "$0.001"
calldata_decode = "$0.001"
log_query       = "$0.001"
block_info      = "$0.001"
