# Per-skill USDC pricing for x402 payment gate.
# Format: "$<amount>" — parsed by x402 PaymentOption.

default = "$0.01"

[skills]
tx_decode       = "$0.02"
receipt_parse   = "$0.02"
trace           = "$0.05"
calldata_decode = "$0.01"
log_query       = "$0.03"
block_info      = "$0.01"
